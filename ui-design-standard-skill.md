# มาตรฐานการออกแบบ UI ของระบบ (Anti-Slop UI Design Standard)

เอกสารนี้เป็นมาตรฐานกลางสำหรับใช้ตัดสินใจเรื่อง UI ทุกหน้าในระบบ เขียนขึ้นเพื่อแก้ปัญหาการออกแบบที่ดูเหมือนงานที่ AI สร้างแบบทั่วไป (ซ้ำแพทเทิร์นเดิม ใช้แค่ Card กับ Button กับ Input วนไปทั้งระบบ) ให้เปลี่ยนไปใช้ชุด component ที่มีอยู่จริงให้ตรงกับประเภทเนื้อหา และให้ทุกหน้าอยู่บนมาตรฐานเดียวกัน

ไลบรารีอ้างอิงคือ MUI (Material UI) เท่านั้น ห้ามผสมไลบรารี UI อื่นหรือสร้าง component ซ้ำเองถ้า MUI มีให้ใช้อยู่แล้ว

---

## 1. หลักคิดหลัก

ให้ทุกหน้ามีจุดเด่นจริงจุดเดียว ส่วนที่เหลือให้เรียบและสงบ ก่อนเพิ่มของตกแต่งใดๆ ให้ถามว่าองค์ประกอบนี้ทำหน้าที่สื่อความหมายอะไร ถ้าตอบไม่ได้ให้ตัดออก

ลำดับความสำคัญของเนื้อหา (visual hierarchy) ต้องมาจากความสำคัญของข้อมูลจริง ไม่ใช่มาจากความสะดวกในการก็อปปี้แพทเทิร์นเดิมซ้ำ

---

## 2. ข้อห้าม จุดที่ทำให้ UI ดูเป็นงานอัตโนมัติทั่วไป (Anti-Slop Checklist)

ห้ามทำสิ่งเหล่านี้เว้นแต่มีเหตุผลเฉพาะจุดจริงๆ

- การ์ดทุกใบหน้าตาเหมือนกันหมด เช่น bo-radius เท่ากัน เงาสีเทาอ่อนเหมือนกันทุกใบ โดยไม่สนใจว่าการ์ดไหนสำคัญกว่ากัน
- กรอบซ้อนกรอบ หรือพื้นหลังซ้อนพื้นหลังในองค์ประกอบเดียวกัน
- ป้ายกำกับตัวพิมพ์ใหญ่ทั้งหมดเหนือหัวข้อทุกจุดทั้งที่ไม่จำเป็น
- ใช้ gradient สีเป็นของตกแต่งพร่ำเพรื่อโดยไม่มีความหมาย
- ใส่หมายเลขนำหน้าแบบ 01 02 03 ทั้งที่เนื้อหาไม่ใช่ขั้นตอนหรือลำดับเวลาจริง
- ใส่ hover หรือ transition แบบเดียวกันในทุกการ์ดโดยไม่มีเหตุผลเฉพาะ
- ใช้เครื่องหมาย `:` `-` `!` `?` ในข้อความ UI ภาษาไทย
- ผสมไอคอนจากหลายชุดในระบบเดียวกัน
- ใช้อิโมจิใน UI ของระบบงานที่เป็นทางการ
- สร้างกล่องแจ้งเตือน/ป้ายสถานะขึ้นเองด้วย div และสี แทนที่จะใช้ component ที่ MUI มีให้อยู่แล้ว (Alert, Chip, Snackbar)

---

## 3. Typography Hierarchy

กำหนด scale เดียวใช้ทั้งระบบผ่าน MUI Typography ไม่กำหนด font-size แยกเป็นจุดๆ เอง

| ระดับ | การใช้งาน | น้ำหนัก |
|---|---|---|
| H1 | ชื่อหน้าหลัก มีหน้าละ 1 จุด | หนา |
| H2 | หัวข้อหมวดย่อยในหน้า | กึ่งหนา |
| Subtitle | บรรยายรองหัวข้อ | ปกติ สีเทา |
| Body | เนื้อหาทั่วไป | ปกติ |
| Caption | ข้อมูลประกอบเล็กๆ เช่น เวลาที่อัปเดต | ปกติ สีเทาอ่อน |

กฎเพิ่มเติม
- ห้ามเน้นแค่คำเดียวในหัวข้อด้วยตัวเอียง ตัวหนา หรือสีต่างจากส่วนอื่นเว้นแต่จำเป็นจริง
- ห้ามใส่ label กำกับเหนือเนื้อหาที่ไม่ได้ให้ข้อมูลเพิ่ม
- หัวเรื่องของการ์ดหรือ section ต้องมีขนาดสัมพันธ์กับเนื้อหาข้างใน ห้ามให้หัวเรื่องใหญ่กว่าเนื้อหาที่ผู้ใช้ต้องใช้งานจริง

---

## 4. ระยะห่างและกริด (Spacing and Layout)

- ใช้ spacing scale คงที่หน่วยเดียว เช่น ทวีคูณของ 8px ผ่าน MUI `theme.spacing`
- จัดโครงสร้างหน้าด้วย `Box` `Container` `Grid` `Stack` เท่านั้น ห้ามกำหนด margin หรือ padding มือแยกจุด
- กลุ่มเนื้อหาที่เกี่ยวข้องกันต้องอยู่ใกล้กัน และมีระยะห่างจากกลุ่มอื่นที่มองเห็นความแตกต่างได้ชัด
- แถวหรือคอลัมน์ในตารางและฟอร์มต้องเรียงบนเส้นกริดเดียวกันทั้งหน้า

---

## 5. กรอบ เงา และพื้นผิว (Border, Elevation, Surface)

- เลือกใช้อย่างใดอย่างหนึ่งระหว่างเส้นขอบบางกับเงา (elevation) ในองค์ประกอบเดียว ไม่ใช้พร้อมกันทั้งคู่โดยไม่มีเหตุผล
- ห้ามมีกรอบซ้อนกรอบเกิน 1 ชั้น
- ใช้ `Paper` หรือ `Card` พร้อมกำหนดระดับ elevation ต่างกันตามลำดับความสำคัญ ไม่ใช่ทุกการ์ดใช้ค่าเดียวกันหมดทั้งระบบ
- ข้อมูลประกอบเล็กๆ เช่น ป้ายปีการศึกษาหรือภาคเรียน ไม่ต้องใส่กรอบหรือพื้นหลังเน้น ให้แสดงเป็นข้อความหรือ Chip ธรรมดา

---

## 6. สีและคอนทราสต์

- กำหนด palette หลักผ่าน MUI theme เท่านั้น ได้แก่ primary, secondary, success, warning, error, neutral ใช้ค่าเดียวกันทุกหน้า
- ใช้คอนทราสต์สูงเฉพาะจุดที่ต้องดึงความสนใจจริง เช่น ปุ่มลบข้อมูล
- คอนทราสต์ของข้อความต่อพื้นหลังต้องผ่านมาตรฐานการเข้าถึงได้ขั้นต่ำ 4.5 ต่อ 1 สำหรับข้อความทั่วไป

---

## 7. ไอคอน (Icons)

- ใช้ MUI Icons ชุดเดียวทั้งระบบเท่านั้น ห้ามผสมไอคอนจากไลบรารีอื่น
- กำหนดขนาดไอคอนคงที่ตามตำแหน่งใช้งาน เช่น ในปุ่มใช้ขนาดหนึ่ง ในรายการใช้อีกขนาดหนึ่ง และใช้ขนาดเดียวกันทุกจุดที่บริบทเหมือนกัน
- ห้ามใช้อิโมจิแทนไอคอนในทุกกรณี

---

## 8. ข้อความภาษาไทยบน UI

- ห้ามใช้เครื่องหมาย `:` `-` `!` `?` ในข้อความ UI ภาษาไทย ให้ปรับประโยคใหม่หรือขึ้นบรรทัดใหม่แทน
- ห้ามใช้อิโมจิ
- เขียนแบบบอกการกระทำตรงๆ เช่นปุ่มกดว่า บันทึกข้อมูล ข้อความแจ้งผลต้องใช้คำเดียวกับปุ่มที่กด เช่น บันทึกข้อมูลแล้ว ไม่ใช้คำอื่นที่ไม่เกี่ยวข้องกัน
- ข้อความแจ้งข้อผิดพลาดต้องบอกว่าเกิดอะไรขึ้นและควรทำอะไรต่อ เขียนตรงประเด็น ไม่ใช้น้ำเสียงขอโทษเวิ่นเว้อ
- หน้าที่ไม่มีข้อมูล เช่น คลังไฟล์ว่างเปล่า ให้ข้อความชวนให้ลงมือทำขั้นต่อไป ไม่ใช่แค่บอกว่าไม่มีข้อมูล

---

## 9. แผนที่การใช้ MUI Component ตามประเภทเนื้อหา

หลักการคือให้เลือก component ตามประเภทของเนื้อหาก่อนเสมอ ห้ามใช้ Card, Button, Text Field ครอบทุกอย่างในระบบ

### Inputs

| Component | ใช้เมื่อ |
|---|---|
| Button | การกระทำหลักของหน้าหรือของ section หนึ่งจุด |
| Button Group | ชุดการกระทำที่เกี่ยวข้องกัน เช่น ตัวเลือกช่วงเวลา |
| Checkbox | เลือกได้หลายรายการ เช่น เลือกไฟล์หลายไฟล์เพื่อลบพร้อมกัน |
| Floating Action Button | การกระทำหลักลอยตัวในหน้าที่เนื้อหายาว เช่น ปุ่มอัปโหลดไฟล์ |
| Number Field | กรอกค่าตัวเลข เช่น ปีการศึกษา จำนวนชั่วโมง |
| Radio Group | เลือกได้ตัวเลือกเดียวจากรายการสั้น เช่น เลือกภาคเรียน |
| Rating | ให้คะแนนหรือประเมินระดับ |
| Select | เลือกจากรายการยาว เช่น เลือกวิชาหรือระดับชั้น |
| Slider | กำหนดค่าตัวเลขแบบช่วง เช่น กรองตามช่วงปี |
| Switch | เปิดหรือปิดสถานะ เช่น เปิดใช้งานบัญชี |
| Text Field | กรอกข้อความสั้นหรือยาว |
| Transfer List | ย้ายรายการระหว่างสองฝั่ง เช่น จัดนักเรียนเข้ากลุ่ม |
| Toggle Button | สลับมุมมองการแสดงผล เช่น สลับตารางกับการ์ด |

### Data display

| Component | ใช้เมื่อ |
|---|---|
| Avatar | รูปประจำตัวครูหรือนักเรียน |
| Badge | ตัวเลขแจ้งเตือนหรือสถานะเล็กติดมุมไอคอน |
| Chip | แท็กหมวดหมู่หรือสถานะสั้น แทนการสร้างกรอบและพื้นหลังเอง |
| Divider | แบ่งส่วนเนื้อหา แทนการวาดเส้นหรือกรอบขึ้นเอง |
| Icons / Material Icons | ไอคอนมาตรฐานทั้งระบบ |
| List | รายการแนวตั้ง เช่น รายการไฟล์ในคลัง |
| Table | ข้อมูลตารางขนาดเล็กถึงกลาง เช่น รายชื่อนักเรียนในหน้าเดียว |
| Tooltip | คำอธิบายเพิ่มเติมเมื่อชี้เมาส์ แทนข้อความล้นพื้นที่ |
| Typography | ควบคุม hierarchy ของข้อความทั้งหมด ห้ามเซ็ตขนาดตัวอักษรแยกเอง |

### Feedback

| Component | ใช้เมื่อ |
|---|---|
| Alert | แจ้งสถานะในหน้า เช่น สำเร็จ ผิดพลาด คำเตือน แทนกล่องสีที่สร้างเอง |
| Backdrop | พื้นหลังทึบระหว่างโหลดหรือเปิดหน้าต่างลอย |
| Dialog | ยืนยันการกระทำสำคัญ เช่น ยืนยันลบไฟล์ |
| Progress | แสดงสถานะระหว่างอัปโหลดหรือโหลดข้อมูล |
| Skeleton | แสดงระหว่างรอโหลดข้อมูล แทนพื้นที่ว่างเปล่า |
| Snackbar | แจ้งผลลัพธ์ชั่วคราว เช่น บันทึกข้อมูลแล้ว |

### Surfaces

| Component | ใช้เมื่อ |
|---|---|
| Accordion | ย่อขยายเนื้อหายาว เช่น รายละเอียดของแต่ละหมวดหมู่ |
| App Bar | แถบบนสุดของระบบ |
| Card | กลุ่มเนื้อหาที่เกี่ยวข้องกันจริงๆ ไม่ใช้พร่ำเพรื่อทุกจุดของหน้า |
| Paper | พื้นผิวยกระดับสำหรับ container ทั่วไปที่ไม่ต้องมีโครงสร้างของ Card |

### Navigation

| Component | ใช้เมื่อ |
|---|---|
| Bottom Navigation | เมนูหลักของหน้าจอมือถือ |
| Breadcrumbs | บอกตำแหน่งหน้าปัจจุบัน เช่น หน้าแรก จากนั้น คลังไฟล์ |
| Drawer | เมนูด้านข้างของระบบ |
| Link | ลิงก์ข้อความ |
| Menu / Menubar | เมนูตัวเลือกที่เปิดจากการคลิก |
| Pagination | แบ่งหน้าตารางหรือรายการยาว |
| Speed Dial | ปุ่มลัดหลายฟังก์ชันลอยตัว |
| Stepper | ขั้นตอนที่เป็นลำดับจริง เช่น ขั้นตอนการอัปโหลดหลักฐาน |
| Tabs | สลับมุมมองของข้อมูลชุดเดียวกัน |

### Layout

| Component | ใช้เมื่อ |
|---|---|
| Box / Container / Grid / Stack | โครงสร้างเลย์เอาต์หลักของทุกหน้า แทนการกำหนด margin padding มือ |
| Image List | แสดงรูปภาพเป็นตาราง เช่น ภาพกิจกรรมการอบรม |

### Utils

| Component | ใช้เมื่อ |
|---|---|
| Modal / Popover / Popper | หน้าต่างลอยหรือเมนูตามบริบท |
| Textarea Autosize | ช่องข้อความยาวที่ขยายความสูงอัตโนมัติ |
| useMediaQuery | ปรับ layout ตามขนาดหน้าจอ |

### MUI X

| Component | ใช้เมื่อ |
|---|---|
| Data Grid | ตารางข้อมูลจำนวนมาก ต้องกรองหรือเรียงลำดับ เช่น รายชื่อนักเรียนทั้งวิทยาลัย |
| Date and Time Pickers | เลือกวันที่หรือเวลา |
| Charts | กราฟสรุปข้อมูล เช่น สถิติการอัปโหลดหลักฐาน |
| Tree View | โครงสร้างหมวดหมู่แบบลำดับชั้น เช่น คลังไฟล์ที่มีหมวดย่อย |

### Lab

| Component | ใช้เมื่อ |
|---|---|
| Masonry | จัดวางรูปภาพที่ขนาดไม่เท่ากัน |
| Timeline | แสดงเหตุการณ์ตามลำดับเวลา เช่น ประวัติการอัปโหลด |

---

## 10. กฎการเลือก Component เพื่อไม่ให้วนใช้แค่ Card, Button, Input

ก่อนวางองค์ประกอบใดๆ ให้ถามก่อนว่าเนื้อหานี้เป็นประเภทไหน แล้วเลือกตามตารางในข้อ 9 ไม่ใช่เลือก Card เป็นค่าเริ่มต้นเสมอ

- สถานะสั้นๆ เช่น ใช้งาน รอดำเนินการ ผ่าน ไม่ผ่าน ให้ใช้ Chip ไม่สร้างกรอบสีขึ้นเอง
- ตัวเลขหรือสถิติสรุป ให้ใช้ Typography ร่วมกับ Icon ไม่ต้องมีกรอบเว้นแต่เป็นจุดเด่นจริงของหน้านั้น
- การแจ้งผลการทำงานทุกครั้ง ให้ใช้ Snackbar หรือ Alert เท่านั้น ห้ามสร้างกล่องข้อความขึ้นเอง
- รายการยาวให้เลือกระหว่าง List, Table หรือ Data Grid ตามปริมาณและความซับซ้อนของข้อมูล ไม่ใช่เรียงการ์ดต่อกันแทนตาราง
- ขั้นตอนที่ต้องทำตามลำดับจริงให้ใช้ Stepper ไม่ใช้การ์ดเรียงพร้อมตัวเลขกำกับเอง

---

## 11. คุณภาพขั้นต่ำที่ต้องมีทุกหน้า

- แสดงผลได้ดีบนจอมือถือ
- มองเห็นตำแหน่งโฟกัสของคีย์บอร์ดได้ชัดเจน
- คอนทราสต์สีผ่านมาตรฐานการเข้าถึงได้
- ใช้ theme และ spacing scale เดียวกันทุกหน้า ไม่กำหนดแยกเป็นหน้าๆ

---

## 12. เช็กลิสต์ก่อนถือว่างานเสร็จ

- [ ] ไม่มีกรอบซ้อนกรอบเกิน 1 ชั้นในองค์ประกอบเดียว
- [ ] หัวเรื่องของหน้าไม่ใหญ่กว่าเนื้อหาที่ผู้ใช้ต้องใช้งานจริง
- [ ] ใช้ icon set เดียวทั้งหน้า
- [ ] ไม่มีอิโมจิในหน้า
- [ ] ไม่มีเครื่องหมาย `:` `-` `!` `?` ในข้อความภาษาไทย
- [ ] ทุกจุดที่มีกรอบหรือพื้นหลังเน้น อธิบายเหตุผลได้ว่าทำไมต้องเน้น
- [ ] ปุ่มและ typography ใช้มาตรฐานเดียวกับหน้าอื่นในระบบ
- [ ] เลือก component ตรงกับประเภทเนื้อหาตามข้อ 9 ไม่ใช่ Card ทุกอย่าง
- [ ] สถานะสั้นใช้ Chip ผลลัพธ์การทำงานใช้ Snackbar หรือ Alert ไม่สร้างกล่องเอง
