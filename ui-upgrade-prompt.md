# คำสั่งปรับปรุง UI ระบบ (UI Upgrade Prompt)

> สถานะ: ฉบับร่าง กำลังรวบรวมปัญหาเพิ่มเติม จะอัปเดตไฟล์นี้ไปเรื่อยๆ จนครบแล้วค่อยนำไปสั่งงาน AI ที่จะแก้โค้ดจริง

## เป้าหมายรวม

ปรับปรุงหน้าตาและโครงสร้าง UI ของระบบทั้งหมดให้ดูเป็นมืออาชีพมากขึ้น โดยยึดหลัก UI/UX design principles มาตรฐาน (hierarchy, progressive disclosure, consistency, contrast, accessibility, proximity, alignment) เน้นให้ content เป็นจุดสนใจหลักของหน้า ลดองค์ประกอบตกแต่งที่ไม่จำเป็น และทำให้ทุกหน้าในระบบมีแพทเทิร์นเดียวกัน

---

## หลักการออกแบบที่ต้องยึดถือ

1. **Hierarchy** — จัดลำดับความสำคัญของข้อมูลด้วยขนาดตัวอักษร น้ำหนักตัวอักษร คอนทราสต์ และระยะห่าง ให้ผู้ใช้รู้ทันทีว่าอะไรสำคัญที่สุดในหน้านั้น
2. **Progressive Disclosure** — แสดงข้อมูล/ตัวเลือกทีละส่วนตามลำดับการใช้งาน ไม่ยัดทุกอย่างไว้พร้อมกันจนดูรก
3. **Consistency** — องค์ประกอบแบบเดียวกัน (ปุ่ม การ์ด ไอคอน) ต้องหน้าตาและพฤติกรรมเหมือนกันทุกหน้า
4. **Contrast** — ใช้คอนทราสต์สูงเฉพาะจุดที่ต้องการดึงความสนใจจริงๆ เช่น ปุ่มลบ/ปุ่มยืนยันสำคัญ ไม่ใช่ใช้พร่ำเพรื่อ
5. **Accessibility** — คอนทราสต์สี ขนาดตัวอักษร และการรองรับการใช้งานต้องเป็นไปตามมาตรฐานการเข้าถึงได้
6. **Proximity** — องค์ประกอบที่เกี่ยวข้องกันต้องอยู่ใกล้กัน แยกองค์ประกอบที่ไม่เกี่ยวข้องออกจากกันชัดเจน
7. **Alignment** — จัดวางตามกริดให้เป็นระเบียบ เส้นขอบและคอลัมน์ต้องตรงกัน

---

## ปัญหาที่พบและสิ่งที่ต้องแก้ไข

### 1. Header/การ์ดหัวเรื่องใหญ่เกินไป แย่ง focus จาก content

**ปัญหา:** การ์ดหัวเรื่องขนาดใหญ่ (เช่น การ์ด "ทางลัดอัปโหลดด่วน (Quick Upload Grid)") กินพื้นที่หน้าจอเกือบครึ่งหน้า ทั้งที่ตัวหัวเรื่องเองไม่ได้ให้ข้อมูลที่ผู้ใช้ต้องใช้งานจริง ผู้ใช้ควรเห็น content/รายการหมวดหมู่เป็นสิ่งแรก ปัญหานี้เกิดซ้ำในแทบทุกหน้าของระบบ

**สิ่งที่ต้องทำ:**
- ลดขนาดของ header/hero card ลงอย่างมาก ให้เหลือแค่บอกบริบทสั้นๆ
- ย้าย focus ของหน้าไปที่ content หลัก (การ์ดหมวดหมู่ ตาราง ฯลฯ) ให้เห็นตั้งแต่ viewport แรก
- กำหนดสัดส่วน header : content ใหม่เป็นมาตรฐานเดียวใช้ทุกหน้า ไม่ใช่แก้เฉพาะหน้านี้หน้าเดียว

### 2. กรอบ/พื้นหลังเน้นจุดที่ไม่จำเป็น และกรอบซ้อนกรอบหลายชั้น

**ปัญหา:** มีองค์ประกอบเล็กๆ เช่น ป้าย "รอบข้อมูลปัจจุบัน: ปีการศึกษา 2568 (ภาคเรียนที่ 1)" ถูกใส่กรอบและพื้นหลังทำให้ดูเด่นเกินจริง ทั้งที่เนื้อหานี้ไม่ใช่สิ่งที่ผู้ใช้ต้องโฟกัส จุดลักษณะนี้กระจายอยู่หลายจุดทั่วระบบ นอกจากนี้บางการ์ดมีเส้นกรอบซ้อนกันหลายชั้น (border ซ้อน border) ทำให้ดูรกและไม่เป็นระเบียบ

**สิ่งที่ต้องทำ:**
- ไล่ทบทวนทุกจุดที่ใส่กรอบ/badge/พื้นหลังเน้น คงไว้เฉพาะจุดที่ต้องการดึงความสนใจจริงๆ ตามหลัก contrast
- ห้ามมีกรอบซ้อนกรอบเกิน 1 ชั้นในองค์ประกอบเดียวกัน
- ลด visual noise โดยรวม ให้ hierarchy สะท้อนความสำคัญของข้อมูลจริง ไม่ใช่ทำให้ทุกอย่างเด่นเท่ากันหมด

### 3. Typography hierarchy และปุ่ม (buttons) ทั้งระบบ

**สิ่งที่ต้องทำ:**
- กำหนด typography scale มาตรฐาน (หัวข้อหลัก หัวข้อรอง เนื้อหา caption) และบังคับใช้ให้เหมือนกันทุกหน้า
- กำหนดสไตล์ปุ่มมาตรฐาน (primary / secondary / danger) ขนาด สี และ state (hover, disabled) ให้เหมือนกันทุกจุดในระบบ

### 4. ตารางและการจัดวางองค์ประกอบ

**สิ่งที่ต้องทำ:**
- จัดระยะห่างคอลัมน์/แถวในตารางให้อ่านง่าย ใช้หลัก proximity และ alignment
- ตรวจสอบการจัดวางองค์ประกอบทั้งหน้าให้อยู่บนกริดเดียวกัน ไม่เยื้องไม่เอียง

### 5. การใช้เครื่องหมายวรรคตอนและไอคอนในข้อความภาษาไทย

**ปัญหา:** ข้อความ UI ภาษาไทยมีการใช้เครื่องหมาย `:` `-` `!` `?` ซึ่งไม่ใช่ธรรมเนียมการเขียนภาษาไทยและดูไม่เป็นมืออาชีพ รวมถึงมีอิโมจิปะปนอยู่ในบางจุด และไอคอนที่ใช้มาจากหลายชุด (icon set) ทำให้หน้าตาไม่เป็นมาตรฐานเดียวกัน

**สิ่งที่ต้องทำ:**
- เอาเครื่องหมาย `:` `-` `!` `?` ออกจากข้อความ UI ภาษาไทยทั้งหมด ปรับประโยค/ใช้การเว้นวรรคหรือขึ้นบรรทัดใหม่แทน
- เอาอิโมจิออกจาก UI ทั้งหมด
- เลือก icon set เดียวใช้ทั้งระบบเท่านั้น ห้ามผสมชุดไอคอนจากหลายแหล่ง

---

## รอเพิ่มเติม

*(จุดนี้ไว้ใส่ปัญหาถัดไปที่ยังไม่ได้รวบรวม — จะทยอยเพิ่มทีละข้อ)*
